server.port=8088



# ???????
lumina.gateway.port=9090
lumina.gateway.max-connections=10000
lumina.gateway.idle-timeout=30
lumina.gateway.heartbeat-interval=10
lumina.gateway.thread-pool.core-size=10
lumina.gateway.thread-pool.max-size=50
lumina.gateway.buffer-size=8192

# Redis ??
lumina.redis.host=localhost
lumina.redis.port=6379
lumina.redis.database=0
lumina.redis.timeout=3000
lumina.redis.pool.max-active=100
lumina.redis.pool.max-idle=20
lumina.redis.pool.min-idle=5

# MySQL ??
#lumina.mysql.url=jdbc:mysql://localhost:3306/lumina_game?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
#lumina.mysql.username=root
#lumina.mysql.password=6543210
#lumina.mysql.driver-class-name=com.mysql.cj.jdbc.Driver

# ??Spring Boot?????
spring.datasource.url=jdbc:mysql://localhost:3306/lumina_game?useSSL=false&serverTimezone=UTC&characterEncoding=utf8
spring.datasource.username=root
spring.datasource.password=6543210
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

lumina.mysql.pool.maximum-pool-size=20
lumina.mysql.pool.minimum-idle=5
lumina.mysql.pool.connection-timeout=30000
lumina.mysql.pool.idle-timeout=600000
lumina.mysql.pool.max-lifetime=1800000

# WebRTC ??
lumina.webrtc.stun-servers=stun:stun.l.google.com:19302,stun:stun1.l.google.com:19302
lumina.webrtc.turn-servers=turn:localhost:3478
lumina.webrtc.ice-timeout=10000
lumina.webrtc.negotiation-timeout=15000

# ????
lumina.match.queue-timeout=60
lumina.match.max-players-per-room=8
lumina.match.min-players-per-room=2

# ????
lumina.room.tick-rate=20
lumina.room.max-rooms=1000
lumina.room.room-timeout=300

# ?????
lumina.anticheat.max-input-rate=30
lumina.anticheat.max-speed=10.0
lumina.anticheat.position-tolerance=1.0
